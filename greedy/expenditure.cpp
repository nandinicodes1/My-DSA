#include<bits/stdc++.h>
#include<queue>
using namespace std;

int 
int main()
{
	int n;
	cin>>n;
	vector<int,int> v;
	for(int i=0;i<n;i++)
	{
		int x,y;
		cin>>x>>y;
		v.push_back(make_pair(x,y));
	}
	int L,P;
	cin>>L>>P;
	
	
	
}
